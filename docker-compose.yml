version: '3.8'
services:
  redis:
    image: redis:latest
    container_name: smart_ambulance_redis
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:
