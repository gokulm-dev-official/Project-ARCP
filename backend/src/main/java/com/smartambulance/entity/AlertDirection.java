package com.smartambulance.entity;

/**
 * Direction the ambulance is approaching from, relative to the vehicle.
 */
public enum AlertDirection {
    BEHIND,
    AHEAD,
    LEFT,
    RIGHT,
    APPROACHING,
    PASSING,
    CLEARED
}
